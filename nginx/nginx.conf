server {
    listen 80;

    server_name nexusflow.works www.nexusflow.works;

    location / {
        proxy_pass http://frontend;
    }

    location /api/ {
        proxy_pass http://backend:8080;
    }
}